# Generated by tools/m12_system_quarantine.py --write (cabinet_actions).
# Installed source SHA256: 175021b14fde0e5e7eaaa6e8a43b983282f69196ae212d8bb6997ba82c9f10aa
# Definitions stay resolvable but cannot enter AD 1 gameplay.
pop_promote_action = {
	ability = adm

	potential = {
		always = no # ANTIQVITAS mounted-system quarantine
		NOT = { has_variable = serf_social_integration_variable }
		num_locations > 0
	}



	select_trigger = {
		looking_for_a = province
		source = actor
		target_flag = target
		name = "promote_action_select_province"
		none_available_msg_key = "no_valid_provinces"
		column = {
			data = name
		}
		column = {
			data = missing_promotions
		}
		column = {
			data = peasants_unemployment
		}
		default_sort = missing_promotions
		map_color = define:NMapColors|MAP_COLOR_HIGH
	}

	location_modifier = {
		local_pop_promotion_speed = 0.005
	}

	map_marker = {
		show_for_owner = yes
	}

	progress = {
		scope:target = {
			value = "unfilled_jobs_in_province_percentage"
		}
	}
}
