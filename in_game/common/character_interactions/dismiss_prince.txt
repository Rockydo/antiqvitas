# Generated by tools/m12_system_quarantine.py --write (character_interactions).
# Installed source SHA256: d781bbcfb5c5fde85d6212869f1c4b14387fd61fd9bbb291c9ff0e2d982fa7ec
# Definitions stay resolvable but cannot enter AD 1 gameplay.
dismiss_prince = {
	is_consort_action = no
	on_own_nation = yes

	message = yes


	potential = {
		always = no # ANTIQVITAS mounted-system quarantine
		scope:actor = {
			succession_law = heir_selection:veche_selection
		}
	}

	ai_tick = never
	ai_tick_frequency = 30
	allow = {
		scope:actor = {
			OR = {
				government_power >= 10
				stability < 0
			}
		}
	}

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = recipient
		name = "choose_character"
		column = {
			data = name
		}
		visible = {
			is_ruler = yes
			is_alive = yes
			scope:actor = {
				ruler = root
			}
		}
		enabled = {
		}
	}

	ai_will_do = {
		add = {
			value = -15
		}
		add = {
			if = {
				limit = {
					scope:recipient = {
						NOT = { culture = scope:actor.culture }
					}
				}
				value = 10
			}
		}
		add = {
			if = {
				limit = {
					scope:recipient = {
						NOT = { religion = scope:actor.religion }
					}
				}
				value = 10
			}
		}
	}

	effect = {
		scope:actor = {
			if = {
				limit = {
					stability > 0
				}
				add_government_power = government_power_extreme_penalty
			}
			add_prestige = prestige_extreme_penalty
		}
		scope:recipient = {
			remove_ruler = scope:actor
			set_variable = {
				name = veche_got_dismissed
				value = yes
			}
		}
	}
}
