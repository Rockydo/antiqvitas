# Generated by tools/m12_system_quarantine.py --write (character_interactions).
# Installed source SHA256: dc838a2da608be982d407bd2298f5d47a93966551b75b9b926e351cb8da78128
# Definitions stay resolvable but cannot enter AD 1 gameplay.
seppuku = {

	on_own_nation = yes

	message = yes

	potential = {
		always = no # ANTIQVITAS mounted-system quarantine
		scope:actor = {
			religion = {
				has_purity = yes
			}
		}
	}

	ai_tick = never
	ai_tick_frequency = 180

	allow = {
		scope:actor = { has_ruler = yes }
	}

	select_trigger = {
		looking_for_a = character
		source = actor
		target_flag = recipient
		name = "choose_character"
		column = {
			data = name
		}
		visible = {
		}
		enabled = {
			is_ruler = yes
		}
	}

	effect = {
		kill_character = {
			target = scope:recipient
			reason = seppuku
		}
		scope:actor = {
			add_purity = purity_extreme_bonus
			add_stability = stability_ultimate_penalty
		}
	}

	ai_will_do = {
		add = {
			value = 20
			subtract = scope:actor.purity
		}
		if = {
			limit = {
				scope:actor = { stability < 0 }
			}
			subtract = 25
		}
		if = {
			limit = {
				scope:actor = { legitimacy < 50 }
			}
			subtract = 10
		}
		if = {
			limit = {
				scope:actor.ruler = { age_in_years < 25 }
			}
			subtract = 25
		}
	}

}
