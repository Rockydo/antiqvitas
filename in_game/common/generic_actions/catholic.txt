# Generated by tools/m12_system_quarantine.py --write (generic_actions).
# Installed source SHA256: bf86829da9735fafd2f6e5f034dcd858c275163dc5dc18aa475fb92603ee91d8
# Unretained definitions stay resolvable but cannot enter AD 1 gameplay.

#Actions for Holy Orders
request_aid = {
	type = religious

	sound = UI_action_religion_generic

	ai_tick = never
	ai_tick_frequency = 60

	automation_tick = never
	automation_tick_frequency = 6
	# player_automated_category removed: ANTIQVITAS target-safe automation quarantine

	potential = {
		scope:actor.religion = religion:catholic
		scope:actor = {
			NOT = { tag = PAP }
			has_reform = government_reform:military_order_reform
		}
	}

	allow = {
		papacy_active = yes
		scope:actor = {
			NOR = {
				is_rival_of = c:DUMMY
				is_enemy_of = c:DUMMY
			}
			c:DUMMY = {
				this_opinion_of_prev > 100
			}
		}
		country_exists = c:DUMMY
	}

	cooldown = {
		type = recently_requested_aid
		years = 20
	}

	price = price:request_aid_price

	select_trigger = {
		looking_for_a = religion
		source = actor
		target_flag = recipient
		name = "choose_religion"
		column = {
			data = name
		}
	}

	effect = {
		scope:actor = {
			trigger_event_non_silently = military_order_events.1
		}
	}

	ai_will_do = 0 # ANTIQVITAS generic-action AI isolation
}

#Actions for non-theocratic Catholics
