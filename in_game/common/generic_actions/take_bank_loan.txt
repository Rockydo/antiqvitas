# Generated by tools/m12_system_quarantine.py --write (generic_actions).
# Installed source SHA256: c7c11ec6ecdeb3a8532a759c54bed73bc8408a8ac16c3e5bb7d470916aead730
# Unretained definitions stay resolvable but cannot enter AD 1 gameplay.
take_estate_loan = {
	type = owncountry

	potential = {
	}

	allow = {
		scope:actor = {
			country_estate_loan_size <= country_loan_capacity
			NOT = { is_system_automated = credit }
		}
	}

	ai_tick = never #handled in code

	automation_tick = never
	automation_tick_frequency = 12
	show_message = no

	effect = {
		hidden_effect = {
			scope:actor = {
				create_estate_loan = { }
			}
		}
	}
}
