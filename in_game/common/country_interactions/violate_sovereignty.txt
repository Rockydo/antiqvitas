# Generated by tools/m12_system_quarantine.py --write (country_interactions).
# Installed source SHA256: e67a8b2851351658f23a7f1a37d8d7bf234960a9996179562c27d1f564448e22
# Definitions stay resolvable but cannot enter AD 1 gameplay.
violate_sovereignty =  {
	type = diplomacy
	category = CATEGORY_HOSTILE_ACTIONS

	potential = {
		always = no # ANTIQVITAS mounted-system quarantine
		scope:actor = {
			modifier:allow_diplomacy_violate_sovereignty = yes
		}
	}

	allow = {
	}

	ai_tick = daily
	ai_tick_frequency = {
		if = {
			limit = {
				at_war = yes
			}
			value = 5
		}
		else = {
			value = 180
		}
	}

	select_trigger = {
		looking_for_a = country
		target_flag = recipient
		source_ai_override = actor
		source_flags_ai_override = wants_military_access_in
		cache_interaction_source_list = yes
		name = "choose_country"
		column = {
			data = name
		}
		visible = {
			is_great_power = no
			NOT = { gives_food_access_to = scope:actor }
			NOT = { gives_military_access_to = scope:actor }
		}
        enabled = {
		}
	}

	effect = {
		if = {
			limit = {
				exists = scope:recipient
			}
			scope:recipient = {
				add_opinion = {
					target = scope:actor
					modifier = opinion_violated_sovereignty
				}
			}

			create_relation = {
				first = scope:recipient
				second = scope:actor
				type = relation_type:military_access
				months = 6
			}

			create_relation = {
				first = scope:recipient
				second = scope:actor
				type = relation_type:food_access
				months = 6
			}
		}
	}
	reject_effect = {
		reject_hegemon_ultimatum_effect = { type = country_interaction:violate_sovereignty }
	}
	accept = {
		add = hegemon_demand_base_acceptance
	}
	ai_will_do = {
		#base
		value = -1000
		add = hegemon_demand_base_will_do
		add = {
			if = {
				limit = {
					scope:actor = {
						at_war = yes
						wants_military_access_in = scope:recipient
					}
				}
				value = 1100
			}
		}
	}
}
