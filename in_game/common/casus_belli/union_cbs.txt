# Generated by tools/m12_system_quarantine.py --write (casus_belli).
# Installed source SHA256: 8cfeb3f9947128fc1a0e650d4d616f46b26b3ee5da33a902cf948178e7e34980
# Definitions stay resolvable but cannot enter AD 1 gameplay.
cb_union_independence = {
	create_visible = {
		always = no # ANTIQVITAS mounted-system quarantine
        exists = union
        union ?= {
			has_active_senior_partner = yes
        }
        junior_union_with = { target = scope:target }
	}

	declare_enabled = {
        exists = union
        union ?= {
			has_active_senior_partner = yes
        }
        junior_union_with = { target = scope:target }
	}

	create_enabled = {
		NOT = { has_truce_with = scope:target }
	}

	war_goal_type = defend_capital
}

cb_union_war_for_seniority = {
	create_visible = {
		always = no # ANTIQVITAS mounted-system quarantine
        exists = union
        union ?= {
			has_active_senior_partner = yes
        }
        junior_union_with = { target = scope:target }
	}

	declare_enabled = {
        exists = union
        union ?= {
			has_active_senior_partner = yes
        }
        junior_union_with = { target = scope:target }
	}

	create_enabled = {
        country_economical_base >= {
			value = scope:target.country_economical_base
			divide = 2
		}
		NOT = { has_truce_with = scope:target }
	}

	war_goal_type = take_capital
}
