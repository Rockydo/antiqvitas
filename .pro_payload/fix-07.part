EFINITION_ONLY_CULTURES) - start_cultures)\n"
    )
    if old in text:
        text = text.replace(old, new, 1)
    GENERATOR.write_text(text, encoding="utf-8", newline="\n")


def patch_doc() -> None:
    text = DOC.read_text(encoding="utf-8")
    replacements = {
        "The pass adds **23 definitions** and assigns each one to controlled AD 1\nlocations. It also makes Colchis and Osroene use the new Colchian and Syriac\nprofiles, and changes the Finland regional profile from generic Uralic to\nFinnic. I