# M11 Kanishka-apogee illustration

The retained source is `assets_queue/generated_sources/antq_kanishka_apogee_source.png`;
the 1080x440 master is `assets_queue/generated/antq_kanishka_apogee_1080x440.png`;
the game-facing BC7 DDS is
`main_menu/gfx/interface/illustrations/event/antq_kanishka_apogee.dds`.

The text-free image is generic Central-Asian highland context: an empty basin,
dry grass, stony foothills, a narrow stream, sparse natural shrubs, and distant
snowy mountains. It identifies no person, council, polity, settlement, route,
religious material, date, or outcome. It is not a reconstruction of Kanishka's
Kushan state, court, council, or Silk Road.

`EVENT_IMAGES` in `tools/m10_second_century.py` owns the
`antq_m10_second.2006` link. The inspected 1080x440 BC7 texture passed
`make validate` and enabled-mod `make smoke` with zero new error-log lines.

Sources: ANTIQVITAS master plan sections 8.4, 9, and 20; local EU5
country-event image contract.
