# M10 AD 200-299 Historical Currents

Run date: 2026-07-20
Game: EU5 1.3.1.1, build 24187685
Mode: enabled ANTIQVITAS playset and debug-mode autonomous driver

## Automated result

`tools/m10_third_century.py --check` verifies ten source-labelled currents
from the shared ledger: three situations, one disaster, three dynamic events,
one tag switch, and two formations. The renderer resolves every recipient
through the AD 1 tag map, checks the three cosmetic identities against runtime
tags, and emits each game-script date through `AntqDate`.

`make validate` passed. A fresh enabled-mod `make smoke` launched EU5 with
the relocated user directory and debug mode, reached the rendered-menu
heuristic, and reported zero new `error.log` lines against the accepted
baseline.

## Transition contracts

The Constitutio Antoniniana uses the harvested policy effects to exchange the
Roman `peregrini` adapter for universal citizenship. The Sassanid Revolution
exchanges the Parthian great-house reform for a centralized-monarchy adapter;
Diocletian exchanges the Principate for a Dominate adapter. The Alemanni and
Franks are intentionally in-place, source-qualified identity transitions from
reviewed Germanic mesh anchors, rather than invented early centralized states.
All three cosmetic identities have temporary colors and solid CoAs only; M11
owns the final-art pass.

## Runtime coverage and remaining work

The confirmation diagnosis is superseded: the M7 replay now enters Observer
Mode and reaches maximum speed, then reproduces the shared FSR renderer crash
before a periodic capture. After a material renderer, driver, or game-build change, advance an AI observer to capture the AD
212 citizenship transition, 224 Sassanid revolution, third-century crisis,
and 284 Dominate transition. Static and menu-log gates are green; observer
coverage remains open.
