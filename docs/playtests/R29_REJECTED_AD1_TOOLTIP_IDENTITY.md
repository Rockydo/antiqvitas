# R29 rejected at AD 1 — navigation-tooltip identity

R29 was a fresh, non-debug, 1920×1080 Rome campaign under tree fingerprint
`2131391983c16f58fc8258a1097bde68c39158a0ea2701afa69e3f6e538cc69d`.
The fresh New Game transition, AD 1 country selector, and Rome start were all
visually confirmed. Time never advanced beyond 08:00 on 1 January AD 1.

The remediation that rejected R28 resolved correctly in the live panel:
`Frontiers and Sea Lanes`, `Settlement Charters`, `Settlement Migration`,
`Settlement Upkeep`, `Settlements`, `Recruit Survey Leader`, `Survey Cost`,
`Survey Progress`, and `Commissioned Raiders` were all visible. Hovering the
top navigation control nevertheless exposed the separate stock tooltip title
`Geopolitics` and the action `Open Geopolitics Panel`. That creates two names
for one always-available interface and leaves a modern scholarly label in the
opening UI.

The campaign was stopped while still paused. R29 cannot count toward the final
century gate. The fix must cover every matching system key in the interface,
outliner, message settings, automation, and tutorial layers, then pass the full
validation and paired-smoke chain before another fresh AD 1 start.
