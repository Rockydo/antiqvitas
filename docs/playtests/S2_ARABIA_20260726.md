# S2 Arabia granularity probe — 2026-07-26

The focused probe loaded the AD 1 country-selection map, moved the camera to
Mecca through the locally documented `Camera.Goto` console command, and captured
the rebuilt peninsula. Visible map labels include separate Nabataean,
Chaulotaean, Agraean, Gerrhaean, Cataraean, Omanitae, Kindite, Sabaean,
Himyarite, Qatabani, and Hadrami frames rather than Interior Bedouin Societies.

Static acceptance is pinned by `docs/m12/arabia_granularity.csv`: twelve Arabian
frames cover all 351 ownable peninsula locations, the largest owns 56, every
capital is owned, and all frames resolve a country profile, government/reform,
direct standard, source/confidence contract, and eleven localization clients.
Nabataea's Petra proxy at Shoubak lies just outside the installed
`arabia_region`, so the country-owned total is 352 while the peninsula surface
is 351.

The first new-game initialization exposed 43 unique script-system error types
from dormant medieval international-organization, market, character, and dynasty
predicates. After guarded-scope quarantine, the repeated initialization produced
zero script-system errors. The remaining log contains the hardcoded engine
absent-HRE notice and one audio-arena warning; neither is an ANTIQVITAS script
error. `make validate` and paired vanilla/mod smoke pass.

Evidence (local ignored screenshot):
`docs/screens/S2_ARABIA_20260726/arabia_runtime_clean.png`.
