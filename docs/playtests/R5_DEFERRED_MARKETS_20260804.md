# R5 deferred-market control — 2026-08-04

Fresh non-debug player Rome used an empty AD 1 `market_manager` while retaining
all 72 sourced hub, urban, harbor, and building records. The automated observer
reached 08:00 on 1 February; automatic capital-market construction then
produced three `Getting relation with itself` assertions. The live capture also
shows the resulting `New Market Underway` event.

The route does not repair the monthly fault and removes the historical hub
network, so the canonical seeded generator was restored. Evidence is under
`docs/screens/R5_DEFERRED_MARKETS/`.
