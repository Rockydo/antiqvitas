# M5 Prima Porta villa proxy

Run date: 2026-07-20
Game: EU5 1.3.1.1, build 24187685
Mode: enabled ANTIQVITAS playset and debug-mode autonomous driver

## Result

Added an engine-valid `noble_villa` at the existing Rome city node. Italy's
Ministry of Culture describes Livia's Prima Porta estate as a suburban landed
property serving productive and residential uses; Rome is the documented
regional map proxy for that site.

`make validate` passed. A fresh enabled-mod `make smoke` reached the rendered
menu and reported zero new `error.log` lines against the accepted baseline.

## Scope

The building makes the plan's villa/latifundium surface visible. It does not
claim that Rome itself was Prima Porta, quantify Roman villas or enslaved labor,
or represent a universal Italian agricultural model.
