# M10 AD 97-199 Historical Currents

Run date: 2026-07-20
Game: EU5 1.3.1.1, build 24187685
Mode: enabled ANTIQVITAS playset and debug-mode autonomous driver

## Automated result

`tools/m10_second_century.py --check` verifies 18 source-labelled currents
from the shared ledger: nine situations, two disasters, six dynamic historical
events, and one formation. The renderer resolves each recipient through the
AD 1 tag map and emits every game-script date through `AntqDate`.

`make validate` passed. A fresh enabled-mod `make smoke` launched EU5 with the
relocated user directory and debug mode, reached the rendered-menu heuristic,
and reported zero new `error.log` lines against the accepted baseline.

## Champa formation contract

The AD 192 `champa_formation` event is a genuine dynamic-country creation,
not a notification or cosmetic rename. It uses the locally verified
`create_country_from_location` contract to create the previously unused `CPC`
tag at Amarendrapura, then transfers the other three reviewed Han-Rinan proxy
locations. The formation retains their population ledger's Austronesian
culture/religion adapter and intentionally does not invent an event-date
ruler. Its temporary map color and solid CoA are load-valid M10 scaffolding;
M11 owns the historical final-art pass.

## Runtime coverage and remaining work

No third unchanged attempt was made to enter Observer Mode. The confirmation
modal has already rejected the driver's normalized click and foreground-safe
Enter retry, as recorded in `BLOCKERS.md`; a duplicate attempt would not add
evidence. After a material driver or UI change, advance an AI observer through
the second-century windows and capture the Dacian/Parthian situations, Antonine
plague, Yellow-Turban current, and AD 192 Champa release. Static and menu-log
gates are green; the observer gate remains open.
