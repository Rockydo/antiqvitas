# M12 CoA scope compatibility - 2026-07-21

The installed CoA trigger registry directly compared an actor's government type and queried HRE special status. Generic template evaluation reaches inactive legacy tags in the total-conversion start, where those scopes do not exist. The exact-source renderer changes only four government comparisons to the local optional form and inserts an HRE existence guard in three special-status predicates.

Full make validate and the rendered enabled-mod smoke both passed. The driver then reached the AD 1 selector, enabled Observer, and started the live map: docs/screens/m12_coa_probe/selector_loaded.png, docs/screens/m12_coa_probe/observer_enabled.png, and docs/screens/m12_coa_probe/observer_start.png.

The resulting error log contains zero government_type unset-scope errors, zero government-type comparison errors, zero null HRE special-status errors, and zero CoA-trigger source locations. Pass for the CoA compatibility surface. One unrelated invalid-capital lookup in a vanilla flag definition remains for separate analysis.
