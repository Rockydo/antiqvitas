# M4 Sa Huynh central-Vietnam atlas batch

Date: 2026-07-21

This twenty-third source-led batch maps the central-Vietnam Champa-named area
to a new Sa Huynh archaeological proxy, resolving 29 previously unmapped
controlled locations. The audited ledger now has 312 selectors, 11,684
locations, and 142 mapped cultures; the generated catalogue rises to 163
cultures.

The reviewed archaeological chronologies cover AD 1 and identify Sa Huynh’s
central/coastal-highland context, while also describing a transition to Champa.
The row consequently does not project later Champa statehood, a fixed ethnic
boundary, or a known language onto every location; its Malay language fallback
is an engine adapter only.

`make validate` passed with all 13,552 populated locations and the
230,000.000-thousand target preserved. The enabled-mod `make smoke` reached the
rendered menu, held it through the post-load check, and passed with zero new
normalized error-log lines. The global M4 density target remains open.
