# M4 East Asian highland and Arabian regional-atlas batch

Date: 2026-07-21

This twelfth source-led batch adds thirteen non-overlapping selectors resolving
714 more controlled locations. The audited ledger now has 283 selectors,
10,759 locations, and 125 mapped cultures. It deliberately reuses Wa,
Tibeto-Burman, and Arab rather than creating unsupported local identities.

The Japanese rows exclude Ryukyu; Tibet remains a highland regional proxy; and
the Arabian rows cover the plan's interior Bedouin surface only, preserving the
existing South Arabian and unmapped Gulf/Oman distinctions for later evidence.

`make validate` passed with all 13,552 populated locations and the
230,000.000-thousand target preserved. The enabled-mod `make smoke` passed
with zero new normalized error-log lines. The global M4 density target remains
open.
