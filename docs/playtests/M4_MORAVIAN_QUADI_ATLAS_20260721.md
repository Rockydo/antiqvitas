# M4 Moravian Quadi-atlas batch

Date: 2026-07-21

This twenty-second source-led batch maps Moravia to a new Quadi regional proxy,
resolving 16 previously unmapped controlled locations. The audited ledger now
has 311 selectors, 11,655 locations, and 141 mapped cultures; the generated
catalogue rises to 162 cultures.

Oxford’s classical reference places the Suebic Quadi in Moravia around 8 BCE
and connects them to the Marcomanni. It does not establish a fixed local
frontier or resolve the wider Slovakia area, so the Quadi row remains a
contested Moravia-only population frame.

`make validate` passed with all 13,552 populated locations and the
230,000.000-thousand target preserved. The enabled-mod `make smoke` reached the
rendered menu, held it through the post-load check, and passed with zero new
normalized error-log lines. The global M4 density target remains open.
