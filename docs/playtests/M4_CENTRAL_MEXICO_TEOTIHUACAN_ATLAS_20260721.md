# M4 Central Mexican Teotihuacan atlas batch

Date: 2026-07-21

This twenty-seventh source-led batch maps only the installed Central Mexican
area to the existing Teotihuacano proxy, resolving 45 previously unmapped
controlled locations. The audited ledger now has 316 selectors, 11,849
locations, and 144 mapped cultures; the generated catalogue remains at 165
cultures.

The reviewed archaeology places Teotihuacan's urban construction in the first
century CE, but it does not establish a complete Central Mexican cultural map,
a state boundary, an ancient language label, or a later Mexica/Aztec identity.
The row is therefore a contested Teotihuacan-rise regional frame only.

`make validate` passed with all 13,552 populated locations and the
230,000.000-thousand target preserved. The enabled-mod `make smoke` reached
the rendered menu, held it through the post-load check, and passed with zero
new normalized `error.log` lines. The global M4 density target remains open.
