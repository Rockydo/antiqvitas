# M4 Kama-Perm Permic atlas batch

Date: 2026-07-21

This thirty-fifth source-led batch maps the installed Perm area to the existing
Permic proxy, resolving 22 previously unmapped controlled locations. The
audited ledger now has 324 selectors, 12,011 locations, and 149 mapped
cultures; the existing generated catalogue retains 170 cultures.

The source's archaeological-linguistic reconstruction includes the
campaign-boundary middle/lower Kama and lower Belaya context but does not make
an exact ethnographic map. The row is consequently contested: it does not
identify later Komi or Udmurt groups, claim uniform speech, institutional
continuity, or a fixed territorial boundary.

`make validate` passed with all 13,552 populated locations and the
230,000.000-thousand target preserved. The enabled-mod `make smoke` reached
the rendered menu, held it through the post-load check, and passed with zero
new normalized `error.log` lines. The global M4 density target remains open.
