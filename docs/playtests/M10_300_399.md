# M10 AD 300-399 Historical Currents

Run date: 2026-07-20
Game: EU5 1.3.1.1, build 24187685
Mode: enabled ANTIQVITAS playset and debug-mode autonomous driver

## Automated result

`tools/m10_fourth_century.py --check` verifies fourteen source-labelled
currents from the shared ledger: seven situations, one disaster, four dynamic
events, one formation, and one tag switch. The renderer resolves every
recipient through the AD 1 tag map, checks `HNS`, `ERO`, and `WRE` against the
active runtime map, and emits each date through `AntqDate`.

`make validate` passed. A fresh enabled-mod `make smoke` launched EU5 with the
relocated user directory and debug mode, reached the rendered-menu heuristic,
and reported zero new `error.log` lines against the accepted baseline. The
first smoke pass exposed invalid non-ASCII localization identifiers for the
display key `aksum_meroë`; the renderer now normalizes identifiers to ASCII
while retaining the accented display text. The repeated smoke pass was green.

## Transition contracts

The Armenian and Thessalonican currents use the locally harvested
country-religion effect, with the Armenian chronology explicitly qualified in
`ASSUMPTIONS.md`. Hunnic arrival creates `HNS` at the reviewed Kazan Volga
proxy and applies pressure to the Alans. The 395 current waits for the shared
AD 395 age boundary, changes the continuing Roman state to the Western Roman
identity, forms `ERO` at Constantinople, and transfers the generated,
reviewable late-antique regional envelope in
`docs/m10/eastern_roman_395_locations.csv`. M11 owns the temporary solid-CoA
and color replacements.

## Runtime coverage and remaining work

No unchanged Observer Mode attempt was made. The confirmation modal has
already rejected the driver's normalized click and foreground-safe Enter retry,
as recorded in `BLOCKERS.md`; another duplicate input would add no evidence.
After a material driver or UI change, advance an AI observer to capture the
Armenian/Thessalonican religion transitions, Hunnic formation, and the 395
Roman division. Static and menu-log gates are green; observer coverage remains
open.
