# M10 AD 1-96 Historical Currents

Run date: 2026-07-20  
Game: EU5 1.3.1.1, build 24187685  
Mode: enabled ANTIQVITAS playset and debug-mode autonomous driver

## Automated result

`tools/m10_history.py --check` verifies 28 sourced first-century currents from
the shared ledger: 14 situations, two disasters, ten dynamic historical
events, one formation event, and one tag-switch event. The renderer resolves
every recipient against the generated AD 1 tag map and emits every date through
`AntqDate`.

The AD 70 Second Temple event now uses the locally verified location-scoped
building destruction contract, guarded by a live building check. It completes
the plan-required material transformation without claiming a complete Rabbinic
institutional model.

`make validate` passed. The initial enabled-mod smoke exposed unused event
state variables and missing `.entry` localization for dynamic historical
events; the renderer was corrected, then a fresh `make smoke` passed with zero
new `error.log` lines. The game reached its rendered menu-ready state.

## Runtime coverage and remaining work

The local event manager accepts the generated dynamic-history windows and the
situation/disaster managers load without diagnostics. The Kushan event uses the
installed in-place cosmetic transformation and empire-rank adapter. The Xiongnu
event now uses the installed `create_country_from_location` / unique-tag
contract to release a separately localized Northern Xiongnu (`XNO`) from a
reviewed northern M3 location slice; the continuing country becomes Southern
Xiongnu and the start confederation IO is dissolved. The renderer validates
every released location against the local coordinate index and the AD 1 Xiongnu
ownership block. The source supports the broad north/frontier distinction, not
an exact EU5 border or a named released ruler; the approximation is recorded in
`docs/ASSUMPTIONS.md`.

No third unchanged attempt was made to enter Observer Mode. The current confirmation
modal has already rejected the driver's normalized click and its
foreground-safe Enter retry, as recorded in `BLOCKERS.md`; repeating an
unchanged sequence would not create useful verification. After a material
driver/UI change, advance an AI observer through the first-century windows and
capture the Gaius, Xin, Claudian Britain, the AD 48 Xiongnu split, Year of Four
Emperors, and Dacian current panels. This batch's static and menu-log gates are
green; its observer gate is still open.
