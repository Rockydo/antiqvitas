# M10 AD 400-476 Historical Currents

Run date: 2026-07-20
Game: EU5 1.3.1.1, build 24187685
Mode: enabled ANTIQVITAS playset and debug-mode autonomous driver

## Automated result

`tools/m10_final_century.py --check` verifies all thirteen final ledger rows:
seven situations, one disaster, and five dynamic events. The renderer resolves
prior dynamic `HNS`, `ERO`, and `VND` identities deliberately, validates every
Visigothic and Vandal local seed against the reviewed Roman start mesh, and
derives every script date from `AntqDate`.

`make validate` passed. The first enabled-mod menu smoke caught six real
situation/country scope errors. The final renderer was corrected to use the
locally verified `content_trigger` contract. A second complete validation and
fresh smoke reached the rendered menu with zero new `error.log` lines.

## Transition contracts

The Visigothic release uses a seven-location Toulouse/Aquitanian proxy;
Vandal Africa uses a five-location Tunis proxy for the unavailable local
Carthage key. Hunnic/Constantinopolitan/Vandal later currents target the
states dynamically formed by prior history. The terminal Odoacer event uses
the final usable 476 monthly window while the generated campaign calendar
retains the exact 4 September end. M11 owns final visual art for all temporary
solid CoAs and colors.

## Runtime coverage and remaining work

No unchanged Observer Mode attempt was made. The driver's confirmation modal
has already failed two distinct bounded inputs, as recorded in `BLOCKERS.md`.
After a material driver/UI change, run an AI observer to capture the successor
releases and the finale. Static and menu-log gates are green; observer coverage
remains open.
